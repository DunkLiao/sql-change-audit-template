# Workflow

```
改版需求 → Git 開分支 → Step 1 凍結舊版 → Step 2 黃金樣本
       → 請 AI 改 SQL → Step 3 雙跑 → Step 4 差異報告
       → Step 5 總量驗收 → Step 6 分組差異 → 簽核 Commit
```

## 紅燈條件（任一觸發就要回頭）
- 筆數不一致
- 主鍵欄位被改動
- AI 主動改了非需求範圍的欄位
- 出現任何「不知道為什麼會變」的差異
- 有無法用業務邏輯解釋的金額變動
